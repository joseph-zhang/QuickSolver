# In old version of ubuntu, we use apt-get to install and manage packages.
# New version --> from 16.04, the new apt is used.
# usage:
apt install
apt remove
apt search
apt show
apt update
apt upgrade
apt list –installed
apt list –upgradable
apt edit-sources
apt autoremove
apt purge <package-name>
# These commands are easier to use.
